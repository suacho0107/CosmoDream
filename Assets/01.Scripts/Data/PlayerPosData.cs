using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerPosData
{
    public static Vector3 pos = new Vector3(-7.7f, -1.7f, 0);
    public static int romInx = 0;

    //사용법
    //PlayerPosData.posX=(플레이어).transform.position.x;
    //PlayerPosData.posY=(플레이어).transform.position.y;
    //등등...
}
